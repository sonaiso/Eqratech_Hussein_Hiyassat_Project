name: CI

on:
  push:
    branches: [main, sprint2/gate-unification]
  pull_request:
    branches: [main, sprint2/gate-unification]

jobs:
  python-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package and dev dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Generate enriched operators catalog (required by dataset tests)
        run: |
          PYTHONPATH=src python -m fvafk.c2b.read_inputs_for_enrichment_pipeline
          test -f data/operators_catalog_split_enriched.csv

      - name: Run tests
        run: pytest -q

  coq-skeleton:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Coq
        run: |
          sudo apt-get update
          sudo apt-get install -y coq

      - name: Compile Coq Skeletons
        run: |
          coqc coq/Gates/GateSukun.v
          coqc coq/Gates/GateShadda.v
          coqc coq/Gates/GateTanwin.v
