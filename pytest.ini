[pytest]
# Run from project root so tests/ is found (e.g. cd /path/to/Eqratech_Hussein_Hiyassat_Project then pytest -q)
pythonpath = src .
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
