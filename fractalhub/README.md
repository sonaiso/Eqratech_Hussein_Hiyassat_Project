# FractalHub Dictionary

[![Tests](https://img.shields.io/badge/tests-26%20passed-brightgreen)](tests/)
[![Version](https://img.shields.io/badge/version-v02-blue)](data/fractalhub_dictionary_v02.yaml)
[![License](https://img.shields.io/badge/license-MIT-lightgrey)]()

## نظرة عامة (Overview)

القاموس الموحد لمشروع FractalHub - نظام شامل لتمثيل الوحدات اللغوية العربية والبوابات والقواعد الثابتة وفق إطار TAQI (تقييد دفق الطاقة الصوتية).

*The unified dictionary for the FractalHub project - a comprehensive system for representing Arabic linguistic units, gates, and invariants following the TAQI framework (Constrained Acoustic Energy Flow).*

### الميزات الأساسية (Core Features)

- 🔤 **19 وحدة لغوية** منظمة حسب النوع (حركات، تطريز، أصوات، صرف، رموز)
- 🚪 **12 بوابة** موزعة على 6 طبقات معالجة (C1-C3 للترميز، P1-P3 للصوتيات)
- ⚖️ **5 قواعد ثابتة** مع عمليات إصلاح تلقائية
- 📊 **3 مستويات معرفية** للتحقق من الأدلة (يقين، ظن، شك)
- ✅ **التحقق الآلي** من صحة البنية والمحتوى
- 🧪 **اختبارات شاملة** تغطي جميع المكونات

## جدول المحتويات (Table of Contents)

- [البداية السريعة](#البداية-السريعة-quick-start)
- [البنية والمكونات](#البنية-structure)
- [الاستخدام](#الاستخدام-usage)
- [التحقق والاختبار](#التحقق-من-الصحة-validation)
- [دليل المطور](#المساهمة-contributing)
- [المراجع](#المراجع-references)

## البداية السريعة (Quick Start)

### المتطلبات (Prerequisites)

```bash
# Python 3.8 أو أحدث
python --version

# تثبيت المتطلبات
pip install pyyaml pytest
```

### التثبيت (Installation)

```bash
# استنساخ المشروع
git clone https://github.com/sonaiso/Eqratech_Arabic_Diana_Project.git
cd Eqratech_Arabic_Diana_Project

# التحقق من صحة القاموس
python scripts/validate_dictionary.py fractalhub/data/fractalhub_dictionary_v02.yaml

# تشغيل الاختبارات
pytest tests/test_dictionary_load.py -v
```

### مثال بسيط (Simple Example)

```python
import yaml

# تحميل القاموس
with open('fractalhub/data/fractalhub_dictionary_v02.yaml', 'r', encoding='utf-8') as f:
    dictionary = yaml.safe_load(f)

# الوصول إلى حركة الفتحة
fatha = dictionary['units']['U:DIACRITIC:FATHA']
print(f"الحركة: {fatha['text']}")  # الحركة: َ
```

## الإصدارات (Versions)

### v02 (الحالي / Current)

**التاريخ:** 2026-01-05  
**الملف:** [`data/fractalhub_dictionary_v02.yaml`](data/fractalhub_dictionary_v02.yaml)  
**الحالة:** مستقر (Stable)

#### التغييرات الرئيسية:
- ➕ إضافة بيانات التحكم في الإصدار الصريحة
- ➕ تطبيع البنية مع ترتيب المفاتيح
- ➕ إضافة ذرات الصوتيات (8 حركات)
- ➕ إضافة وحدات التطريز (4 أنواع)
- ➕ تعريفات عمليات الإصلاح/الدلتا
- ➕ تحسين سجل البوابات
- ➕ إضافة مستويات الأدلة المعرفية
- ✅ توافق عكسي كامل مع v01

## البنية (Structure)

القاموس منظم في 9 أقسام رئيسية مرتبة:

```yaml
meta:                 # بيانات التحكم في الإصدار والتوافق
units:                # الوحدات اللغوية الذرية
gates:                # بوابات المعالجة والتحويل
evidence:             # الأدلة والمستويات المعرفية
invariants:           # القواعد الثابتة والقيود
tags:                 # التصنيفات والميتاداتا
mappings:             # التوافق العكسي والتحويلات
repair_operations:    # عمليات الإصلاح التلقائي
unit_delta_ops:       # عمليات التحويل على الوحدات
```

### البيانات الوصفية (Metadata)

يحتوي قسم `meta` على معلومات الإصدار والتوافق:

| الحقل | الوصف | القيمة |
|------|-------|--------|
| `dict_version` | رقم إصدار القاموس | `2` |
| `codec_version` | إصدار نظام الترميز | `1` |
| `schema_version` | إصدار المخطط | `1` |
| `generated_at` | تاريخ الإنشاء (ISO 8601) | `2026-01-05T12:00:00Z` |
| `compatibility` | معلومات التوافق | `v01_supported: true` |
| `changelog` | سجل التغييرات | قائمة بالتحديثات |

### الوحدات (Units)

الوحدات هي العناصر الذرية للمعالجة اللغوية. كل وحدة لها معرّف فريد يبدأ بـ `U:`.

#### أنواع الوحدات (Unit Types)

| النوع | الوصف | العدد | مثال |
|------|-------|-------|------|
| `diacritic` | الحركات والتشكيل | 8 | َ ُ ِ ْ ّ ً ٌ ٍ |
| `prosody` | علامات التطريز والتنغيم | 4 | وصل، وقف، ارتفاع، انخفاض |
| `phoneme` | الأصوات الأساسية | 3 | ء ب ت |
| `morpheme` | الوحدات الصرفية | 2 | ال (التعريف)، ة (التأنيث) |
| `token` | الرموز والعلامات | 2 | مسافة، فاصلة |

#### بنية الوحدة (Unit Structure)

```yaml
U:{TYPE}:{NAME}:
  unit_id: "U:{TYPE}:{NAME}"      # المعرّف الفريد (مطلوب)
  kind: "{type}"                   # النوع (مطلوب)
  text: "{نص}"                     # النص العربي (اختياري)
  features: [list]                 # قائمة الخصائص (اختياري)
  links: [list]                    # روابط لوحدات أخرى (اختياري)
  status: "active|deprecated"      # الحالة (مطلوب)
```

#### مثال كامل (Complete Example)

```yaml
U:DIACRITIC:FATHA:
  unit_id: "U:DIACRITIC:FATHA"
  kind: "diacritic"
  text: "َ"
  features: 
    - "short_vowel"    # حركة قصيرة
    - "a_sound"        # صوت الألف
    - "fatha"          # فتحة
  status: "active"
```

### البوابات (Gates)

البوابات هي نقاط المعالجة والتحويل في السلسلة. كل بوابة لها معرّف فريد يبدأ بـ `G:`.

#### الطبقات (Layers)

البوابات منظمة في 6 طبقات متسلسلة:

| الطبقة | الاسم | الوظيفة | عدد البوابات |
|-------|------|---------|--------------|
| **C1** | Codec | التحقق من الترميز وربط الحركات | 2 |
| **C2** | Invariants | تطبيق القواعد الثابتة والتحقق الصرفي | 2 |
| **C3** | Reference | التحقق المرجعي والدلالي | 2 |
| **P1** | Phonology Basic | قواعد صوتية أساسية (إدغام، سكون) | 2 |
| **P2** | Phonology Advanced | قواعد صوتية متقدمة (وصل، نبر) | 2 |
| **P3** | Prosody | التنغيم والتطريز | 2 |

#### بنية البوابة (Gate Structure)

```yaml
G:{LAYER}:{NAME}:
  gate_id: "G:{LAYER}:{NAME}"     # المعرّف الفريد (مطلوب)
  layer: "{LAYER}"                 # الطبقة (مطلوب)
  description: "{وصف}"             # الوصف (اختياري)
  inputs: [list]                   # المدخلات (مطلوب)
  outputs: [list]                  # المخرجات (مطلوب)
  constraints: [list]              # القيود (اختياري)
  evidence_required: bool          # هل الدليل مطلوب؟ (اختياري)
  penalty: float                   # العقوبة عند الانتهاك (اختياري)
  status: "active|deprecated"      # الحالة (مطلوب)
```

#### مثال كامل (Complete Example)

```yaml
G:P1:DOUBLE_SUKUN:
  gate_id: "G:P1:DOUBLE_SUKUN"
  layer: "P1"
  description: "منع التقاء ساكنين متتاليين"
  inputs: 
    - "phoneme_sequence"
  outputs: 
    - "corrected_sequence"
  constraints:
    - "no_consecutive_sukun"
  evidence_required: true
  penalty: 0.9                      # عقوبة عالية
  status: "active"
```

### القواعد الثابتة (Invariants)

القواعد الثابتة هي قيود لا يجوز انتهاكها في المعالجة. كل قاعدة لها معرّف يبدأ بـ `INV:`.

#### بنية القاعدة (Invariant Structure)

```yaml
INV:{NAME}:
  inv_id: "INV:{NAME}"             # المعرّف الفريد (مطلوب)
  scope: "{scope}"                 # النطاق (مطلوب)
  pattern: "{وصف}"                 # النمط (مطلوب)
  penalty: float                   # العقوبة (مطلوب)
  repair_ops: [list]               # عمليات الإصلاح (اختياري)
  status: "active|deprecated"      # الحالة (مطلوب)
```

#### القواعد المعرّفة (Defined Invariants)

| المعرّف | النطاق | الوصف | العقوبة |
|---------|--------|-------|---------|
| `INV:NO_ORPHAN_DIACRITIC` | C1 | لا حركة بدون حرف أساسي | 0.8 |
| `INV:ARTICLE_FORM` | C2 | أل التعريف قبل الاسم مباشرة | 0.85 |
| `INV:DOUBLE_SUKUN_FORBIDDEN` | derived | لا يجوز التقاء ساكنين | 0.9 |
| `INV:TANWIN_FINAL_ONLY` | C2 | التنوين في نهاية الكلمة فقط | 0.85 |
| `INV:FEMININE_MARKER_AGREEMENT` | C3 | تطابق علامة التأنيث | 0.75 |

### الأدلة والمستويات المعرفية (Evidence & Epistemic Levels)

نظام الأدلة يضمن عدم إنتاج معاني بدون أساس موثوق.

#### المستويات المعرفية (Epistemic Levels)

| المستوى | اليقين | الوصف | المتطلبات |
|---------|--------|-------|-----------|
| **YAQIN** | 1.0 | اليقين - تأكيد كامل | شهادة من مصادر متعددة في المدونة |
| **ZANN** | 0.7 | الظن - ترجيح قوي | مصدر واحد أو اشتقاق قاعدي |
| **SHAKK** | 0.4 | الشك - احتمال ضعيف | استنتاج فقط |

#### قواعد الارتكاز (Anchor Rules)

1. **NO_MEANING_WITHOUT_EVIDENCE**
   - **الوصف:** لا يمكن إنتاج معنى بدون دليل أو معرف سابق
   - **الإنفاذ:** strict
   - **التطبيق:** البوابات الدلالية والتفسيرية

2. **EVIDENCE_PROPAGATION**
   - **الوصف:** الدليل ينتقل ويتراكم عبر السلسلة
   - **الإنفاذ:** soft
   - **التطبيق:** جميع البوابات

### عمليات الإصلاح (Repair Operations)

عمليات تلقائية لإصلاح الانتهاكات في الطبقة المشتقة فقط:

| العملية | الوصف | المعاملات |
|---------|-------|-----------|
| `REPLACE` | استبدال وحدة بوحدة أخرى | target_unit_id, replacement_unit_id |
| `INSERT_VIRTUAL` | إدراج وحدة افتراضية | position, virtual_unit_id |
| `DELETE_VIRTUAL` | حذف وحدة افتراضية | virtual_unit_id |

**قيود:**
- لا تطبق إلا على طبقة DERIVED
- يجب توثيق كل عملية بدليل

## الاستخدام (Usage)

### التحميل الأساسي (Basic Loading)

```python
import yaml
from pathlib import Path

# تحميل القاموس
dict_path = Path("fractalhub/data/fractalhub_dictionary_v02.yaml")
with open(dict_path, 'r', encoding='utf-8') as f:
    dictionary = yaml.safe_load(f)

# التحقق من الإصدار
version = dictionary['meta']['dict_version']
print(f"إصدار القاموس: {version}")  # إصدار القاموس: 2
```

### الوصول إلى الوحدات (Accessing Units)

```python
# الحصول على جميع الحركات
units = dictionary['units']
diacritics = {uid: u for uid, u in units.items() if u['kind'] == 'diacritic'}

print(f"عدد الحركات: {len(diacritics)}")
for uid, unit in diacritics.items():
    print(f"  {uid}: {unit['text']}")
```

### الوصول إلى البوابات (Accessing Gates)

```python
# الحصول على بوابات طبقة P1
gates = dictionary['gates']
p1_gates = {gid: g for gid, g in gates.items() if g['layer'] == 'P1'}

for gid, gate in p1_gates.items():
    print(f"{gid}: {gate['description']}")
```

### التحقق من المستوى المعرفي (Checking Evidence Level)

```python
# الحصول على مستويات اليقين
evidence = dictionary['evidence']
epistemic = evidence['epistemic_levels']

for level, data in epistemic.items():
    print(f"{level}: يقين {data['certainty']} - {data['description']}")
```

## التحقق من الصحة (Validation)

### أداة التحقق (Validator Tool)

```bash
# تشغيل المدقق
python scripts/validate_dictionary.py fractalhub/data/fractalhub_dictionary_v02.yaml

# أو مباشرة (إذا كان executable)
./scripts/validate_dictionary.py fractalhub/data/fractalhub_dictionary_v02.yaml
```

### الفحوصات المطبقة (Validation Checks)

| الفحص | الوصف |
|-------|-------|
| **البنية** | التحقق من وجود جميع المفاتيح المطلوبة |
| **الميتاداتا** | صحة الإصدار والطابع الزمني |
| **الوحدات** | صحة الحقول والمعرفات والتنسيق |
| **البوابات** | صحة الحقول والطبقات والمعرفات |
| **القواعد** | صحة النطاقات والأنماط |
| **التكرار** | عدم وجود معرفات مكررة |
| **التنسيق** | الالتزام بصيغة المعرفات (U:, G:, INV:) |

### رسالة النجاح

```
============================================================
FractalHub Dictionary Validation Report
File: fractalhub/data/fractalhub_dictionary_v02.yaml
============================================================

✅ All validations passed!

============================================================
```

### الاختبارات (Tests)

مجموعة شاملة من 26 حالة اختبار لضمان الجودة.

#### تشغيل الاختبارات (Running Tests)

```bash
# جميع الاختبارات
pytest tests/test_dictionary_load.py -v

# اختبارات محددة
pytest tests/test_dictionary_load.py::TestDictionaryV02 -v

# مع تقرير التغطية
pytest tests/test_dictionary_load.py --cov=fractalhub --cov-report=html
```

#### تغطية الاختبارات (Test Coverage)

| الفئة | عدد الاختبارات | الوصف |
|-------|----------------|-------|
| **التحميل والبنية** | 5 | تحميل وصحة البنية الأساسية |
| **المحتوى** | 6 | وحدات، بوابات، قواعد، أدلة |
| **التوافق** | 2 | التوافق العكسي وعدم الكسر |
| **الهيكل** | 8 | صيغة المعرفات والحقول المطلوبة |
| **الاتساق** | 5 | تطابق المعرفات وعدم التكرار |

#### النتائج المتوقعة

```
======================== 23 passed, 3 skipped in 0.60s =========================

✓ 23 اختبار ناجح
⊘ 3 اختبارات متخطاة (متعلقة بـ v01 غير موجود)
✗ 0 فشل
```

## المساهمة (Contributing)

### إرشادات المساهمة

#### 1. إضافة وحدات جديدة

```yaml
# نموذج الوحدة الجديدة
U:{TYPE}:{NAME}:
  unit_id: "U:{TYPE}:{NAME}"
  kind: "{type}"
  text: "{نص}"
  features: ["{feature1}", "{feature2}"]
  status: "active"
```

**الخطوات:**
1. أضف الوحدة في قسم `units` بترتيب أبجدي
2. تأكد من فرادة `unit_id`
3. اختر `kind` من الأنواع المعرّفة
4. شغّل المدقق: `python scripts/validate_dictionary.py ...`
5. أضف اختبار في `tests/test_dictionary_load.py`

#### 2. إضافة بوابات جديدة

```yaml
# نموذج البوابة الجديدة
G:{LAYER}:{NAME}:
  gate_id: "G:{LAYER}:{NAME}"
  layer: "{LAYER}"
  description: "{وصف}"
  inputs: ["{input1}"]
  outputs: ["{output1}"]
  constraints: ["{constraint1}"]
  evidence_required: true
  status: "active"
```

**الخطوات:**
1. حدد الطبقة المناسبة (C1-C3 أو P1-P3)
2. اتبع نمط التسمية المتسق
3. وثّق القيود والمدخلات/المخرجات بوضوح
4. اختبر في السياق الكامل

#### 3. اتفاقيات الترميز

- **المعرّفات:** استخدم UPPER_CASE مع : فاصل
- **الأسماء:** واضحة وموصّفة باللغة الإنجليزية
- **الأوصاف:** باللغة العربية
- **الحقول:** اتبع البنية المعرّفة بدقة

#### 4. سير عمل المراجعة

```bash
# 1. إنشاء فرع جديد
git checkout -b feature/add-new-units

# 2. إجراء التغييرات
# عدّل fractalhub/data/fractalhub_dictionary_v02.yaml

# 3. التحقق من الصحة
./scripts/validate_dictionary.py fractalhub/data/fractalhub_dictionary_v02.yaml

# 4. تشغيل الاختبارات
pytest tests/test_dictionary_load.py -v

# 5. تحديث السجل
# أضف سطر في meta.changelog

# 6. الالتزام والدفع
git add .
git commit -m "feat: Add new linguistic units"
git push origin feature/add-new-units
```

#### 5. قائمة التحقق (Checklist)

قبل تقديم PR:
- [ ] المدقق يمرّ بنجاح
- [ ] جميع الاختبارات تنجح
- [ ] لا تكرار في المعرّفات
- [ ] التوثيق محدّث
- [ ] السجل (changelog) محدّث
- [ ] لا تغييرات كاسرة (أو موثقة بوضوح)

## الأسئلة الشائعة (FAQ)

### ما هو الفرق بين الوحدة والبوابة؟

**الوحدة (Unit):** عنصر ذري ثابت (مثل حركة، صوت، مورفيم)  
**البوابة (Gate):** نقطة معالجة تحوّل المدخلات إلى مخرجات

### متى أستخدم طبقة C مقابل P؟

- **C (Codec/Core):** للترميز والقواعد الثابتة والمرجع
- **P (Phonological):** للمعالجة الصوتية والتطريز

### كيف أتعامل مع التوافق العكسي؟

استخدم قسم `mappings`:
```yaml
mappings:
  renames:
    OLD_ID: NEW_ID
  deprecations:
    - DEPRECATED_ID
  aliases:
    ALIAS: CANONICAL_ID
```

### ماذا يعني penalty في البوابات؟

**penalty:** قيمة بين 0-1 تمثل شدة العقوبة عند انتهاك القيد. كلما اقتربت من 1، كان الانتهاك أخطر.

## استكشاف الأخطاء (Troubleshooting)

### خطأ: "Failed to load YAML"

**السبب:** بنية YAML غير صحيحة  
**الحل:** تحقق من المسافات البادئة والنقطتين الرأسيتين

### خطأ: "Duplicate unit_id"

**السبب:** معرّف مكرر  
**الحل:** تأكد من فرادة جميع المعرّفات

### تحذير: "ID should start with 'U:'"

**السبب:** تنسيق معرّف غير متسق  
**الحل:** اتبع اتفاقيات التسمية (U:, G:, INV:)

## المراجع (References)

### المواصفات التقنية (Technical Specifications)

- **YAML 1.2:** [yaml.org/spec/1.2/spec.html](https://yaml.org/spec/1.2/spec.html)
- **Arabic Unicode:** [unicode.org/charts/PDF/U0600.pdf](https://unicode.org/charts/PDF/U0600.pdf)
- **UTF-8 Encoding:** [RFC 3629](https://tools.ietf.org/html/rfc3629)

### الأطر النظرية (Theoretical Frameworks)

- **إطار TAQI:** تقييد دفق الطاقة الصوتية (Constrained Acoustic Energy Flow)
- **المستويات المعرفية:** اليقين، الظن، الشك في التراث الإسلامي
- **اللسانيات التوليدية:** نظرية الحكومة والربط (Government and Binding Theory)

### أدوات ذات صلة (Related Tools)

- **PyYAML:** مكتبة Python لمعالجة YAML
- **pytest:** إطار الاختبار المستخدم
- **jsonschema:** للتحقق من المخططات (مخطط مستقبلي)

### مشاريع ذات صلة (Related Projects)

- **Mishkal:** مشكّل نصوص عربية
- **MADAMIRA:** محلل صرفي ونحوي
- **CAMeL Tools:** أدوات معالجة اللغة العربية

## الترخيص (License)

هذا المشروع مرخص تحت [رخصة MIT](../../LICENSE) - انظر ملف LICENSE للتفاصيل.

## الاقتباس (Citation)

إذا استخدمت هذا القاموس في بحثك، يرجى الاستشهاد به:

```bibtex
@misc{fractalhub_dict_v02,
  title={FractalHub Unified Dictionary v02},
  author={Eqratech Arabic Diana Project},
  year={2026},
  url={https://github.com/sonaiso/Eqratech_Arabic_Diana_Project},
  note={Arabic linguistic processing dictionary with TAQI framework}
}
```

## التواصل والدعم (Contact & Support)

- **المشاكل (Issues):** [GitHub Issues](https://github.com/sonaiso/Eqratech_Arabic_Diana_Project/issues)
- **المناقشات (Discussions):** [GitHub Discussions](https://github.com/sonaiso/Eqratech_Arabic_Diana_Project/discussions)
- **البريد الإلكتروني:** [عنوان البريد]

---

<div dir="rtl">

### ملخص سريع (Quick Summary)

**ما هو FractalHub Dictionary؟**  
قاموس موحد لتمثيل الوحدات اللغوية العربية في نظام معالجة متعدد الطبقات.

**لماذا أستخدمه؟**  
- بنية منظمة وموحدة
- تحقق آلي من الصحة
- توافق عكسي مضمون
- تغطية شاملة للمكونات اللغوية

**كيف أبدأ؟**  
```bash
pip install pyyaml pytest
python scripts/validate_dictionary.py fractalhub/data/fractalhub_dictionary_v02.yaml
```

</div>

---

**آخر تحديث:** 2026-01-05  
**الإصدار:** v02  
**الحالة:** ✅ مستقر (Stable)
